

const DonateItems = ({item}) => {

    const {image, title, description, price, text_color} = item
   
    return (
        <>
           <div>
              
           </div>
        </>
    );
};

export default DonateItems;
